services:
  # ─── Django Backend (Web Service) ───────────────────────────────────────────
  - type: web
    name: productive-flow-backend
    env: python
    region: oregon
    plan: free
    buildCommand: "pip install -r backend/requirements.txt && python backend/manage.py migrate && python backend/manage.py collectstatic --no-input"
    startCommand: "cd backend && daphne -b 0.0.0.0 -p $PORT config.asgi:application"
    envVars:
      - key: DEBUG
        value: "False"
      - key: PYTHON_VERSION
        value: 3.10.0
      - key: DATABASE_URL
        fromDatabase:
          name: productive-flow-db
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: ALLOWED_HOSTS
        value: "*"
      - key: CORS_ALLOWED_ORIGINS
        value: "*"
      - key: CORS_ALLOW_ALL_ORIGINS
        value: "True"
      - key: GOOGLE_API_KEY
        sync: false
      - key: FRONTEND_URL
        sync: false

  # ─── React Frontend (Static Site) ───────────────────────────────────────────
  - type: static_site
    name: productive-flow-frontend
    env: node
    region: oregon
    plan: free
    buildCommand: "cd frontend && npm install && npm run build"
    staticPublishPath: "frontend/dist"
    envVars:
      - key: VITE_API_URL
        fromService:
          type: web
          name: productive-flow-backend
          envVarKey: RENDER_EXTERNAL_URL
      - key: VITE_WS_URL
        fromService:
          type: web
          name: productive-flow-backend
          envVarKey: RENDER_EXTERNAL_URL
    routes:
      - type: rewrite
        source: "/*"
        destination: "/index.html"

databases:
  - name: productive-flow-db
    region: oregon
    plan: free
